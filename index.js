import { binaries } from './inputs/day-3.input.js';
import { decode } from './utils/day-3.js';

const test = [1, 2, 3, 2, 4, 1];

// countDepths(depths);
// countWindows(depths);
// countDistance(directions);
decode(binaries);
